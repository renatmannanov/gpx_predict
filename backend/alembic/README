Generic single-database configuration for GPX Predictor.

## Usage

Run migrations:
```bash
cd backend
alembic upgrade head
```

Create new migration:
```bash
alembic revision --autogenerate -m "description"
```

Check current version:
```bash
alembic current
```

Downgrade:
```bash
alembic downgrade -1
```
