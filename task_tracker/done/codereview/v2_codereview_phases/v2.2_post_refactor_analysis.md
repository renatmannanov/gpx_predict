# V2.2 Post-Refactor Analysis

> **Дата:** 2026-01-28
> **Цель:** Анализ выполненного рефакторинга v2 + v2.1, выявление проблем, план верификации

---

## Часть 1: Анализ по фазам

### Фазы 0a-0f: Structure (✅ Выполнено)

| Фаза | Оценка | Что хорошо | Что можно было лучше |
|------|--------|------------|---------------------|
| **0a** | ⭐⭐⭐⭐⭐ | Чистое создание структуры | — |
| **0b** | ⭐⭐⭐⭐⭐ | shared/ создан правильно | — |
| **0c** | ⭐⭐⭐⭐ | hiking перенесён | Re-exports потребовали доп. фикса |
| **0d** | ⭐⭐⭐⭐⭐ | trail_run перенесён | — |
| **0e** | ⭐⭐⭐⭐ | strava разбит хорошо | Сложная фаза, но справились |
| **0f** | ⭐⭐⭐⭐ | gpx/users перенесены | RouteSegmenter потребовал доп. фикса |

**Общий вывод по Phase 0:** Хорошо спланировано, подфазы помогли. Проблема: circular imports при re-exports (0c, 0f) — это типичная ошибка, теперь задокументирована в skill.

---

### Фазы 1-7: Main Refactor (✅ Выполнено)

| Фаза | Оценка | Что хорошо | Что можно было лучше |
|------|--------|------------|---------------------|
| **1** | ⭐⭐⭐⭐⭐ | Docker + PostgreSQL с начала | — |
| **2** | ⭐⭐⭐⭐⭐ | Naming унифицирован, enums созданы | — |
| **3** | ⭐⭐⭐⭐⭐ | Формулы в shared/, дублирование убрано | — |
| **4** | ⭐⭐⭐⭐⭐ | Bot cleanup отличный | — |
| **5** | ⭐⭐⭐⭐⭐ | Repository pattern внедрён | — |
| **6** | ⭐⭐⭐⭐⭐ | API унифицирован | — |
| **7** | ⭐⭐⭐⭐⭐ | Тесты обновлены | — |

**Общий вывод по фазам 1-7:** Выполнено хорошо, зависимости между фазами соблюдены.

---

### Фазы 8-11: Cleanup v2.1 (✅ Выполнено)

| Фаза | Оценка | Что хорошо | Что можно было лучше |
|------|--------|------------|---------------------|
| **8** | ⭐⭐⭐⭐⭐ | Stubs удалены | — |
| **9** | ⭐⭐⭐⭐⭐ | Импорты обновлены | — |
| **10** | ⭐⭐⭐⭐⭐ | Async migration завершена | Самая сложная фаза, хорошо задокументирована |
| **11** | ⭐⭐⭐⭐ | Shared formatters (Вариант A) | Компромиссное решение — ок |

---

## Часть 2: Выявленные проблемы

### 2.1 Legacy файлы (НЕ удалены)

После рефакторинга остались старые файлы для backward compatibility. Их нужно либо удалить, либо оставить как re-export.

#### В `backend/app/services/`:
```
services/
├── __init__.py          # re-export? проверить
├── calculators/
│   ├── __init__.py      # re-export из features/
│   ├── base.py          # LEGACY - перенесён в shared/calculator_types
│   ├── comparison.py    # ИСПОЛЬЗУЕТСЯ? проверить
│   └── segmenter.py     # LEGACY - перенесён в features/gpx/
├── gpx_parser.py        # LEGACY - перенесён в features/gpx/
├── naismith.py          # ОСТАВИТЬ - old_naismith (3-й метод расчёта!)
├── prediction.py        # ИСПОЛЬЗУЕТСЯ - кросс-фичёвый сервис
├── strava.py            # LEGACY - перенесён в features/strava/
├── strava_sync.py       # LEGACY - перенесён в features/strava/sync/
├── sun.py               # ИСПОЛЬЗУЕТСЯ? проверить
└── user_profile.py      # ИСПОЛЬЗУЕТСЯ? проверить
```

#### В `backend/app/models/`:
```
models/
├── __init__.py          # re-export
├── base.py              # ОСТАВИТЬ - Base класс для всех моделей
├── gpx.py               # LEGACY/re-export → features/gpx/
├── notification.py      # LEGACY/re-export → features/users/
├── prediction.py        # ИСПОЛЬЗУЕТСЯ? проверить
├── strava_activity.py   # LEGACY/re-export → features/strava/
├── strava_token.py      # LEGACY/re-export → features/strava/
├── user.py              # LEGACY/re-export → features/users/
├── user_profile.py      # LEGACY/re-export → features/hiking/
└── user_run_profile.py  # LEGACY/re-export → features/trail_run/
```

#### В `backend/app/repositories/`:
```
repositories/
├── __init__.py
└── gpx.py               # LEGACY - перенесён в features/gpx/
```

### 2.2 CLAUDE.md устарел

Текущий CLAUDE.md описывает старую структуру:
- Указывает `services/calculators/` вместо `features/hiking/calculators/`
- Указывает `gpx_parser.py` и `segmenter.py` для haversine (теперь в shared/)
- Список проблем включает уже исправленные

### 2.3 README.md созданы, но нужно проверить актуальность

README существуют в:
- ✅ features/gpx/README.md
- ✅ features/hiking/README.md
- ✅ features/hiking/calculators/README.md
- ✅ features/strava/README.md
- ✅ features/trail_run/README.md
- ✅ features/trail_run/calculators/README.md
- ✅ features/users/README.md

---

## Часть 3: План верификации

### 3.1 Автоматическая проверка (уже сделано)

```bash
# Тесты - PASSED (208 tests)
cd backend && python -m pytest tests/ -v

# Backend import - OK
python -c "from app.main import app; print('OK')"
```

### 3.2 Ручная проверка Backend API

```bash
# 1. Запустить PostgreSQL
docker-compose up -d postgres

# 2. Применить миграции
cd backend && alembic upgrade head

# 3. Запустить backend
uvicorn app.main:app --reload

# 4. Проверить endpoints (в другом терминале):

# Health
curl http://localhost:8000/api/v1/health

# GPX upload (нужен файл)
curl -X POST http://localhost:8000/api/v1/gpx/upload -F "file=@test.gpx"

# Users
curl http://localhost:8000/api/v1/users/test123

# Profiles (новые endpoints)
curl http://localhost:8000/api/v1/profiles/test123/hiking
curl http://localhost:8000/api/v1/profiles/test123/trail-run

# Strava status
curl http://localhost:8000/api/v1/strava/status/test123
```

### 3.3 Ручная проверка Bot

```bash
# 1. Запустить бот
cd bot && python main.py

# 2. В Telegram:
# - /start - онбординг работает?
# - Отправить GPX файл - парсится?
# - /strava - подключение работает?
# - /profile - показывает профиль?
```

### 3.4 Проверка импортов (критично!)

```bash
cd backend

# Новые пути (должны работать)
python -c "from app.features.hiking.calculators import ToblerCalculator; print('OK')"
python -c "from app.features.trail_run.calculators import GAPCalculator; print('OK')"
python -c "from app.features.gpx import GPXParserService; print('OK')"
python -c "from app.features.strava import StravaClient; print('OK')"
python -c "from app.shared import haversine; print('OK')"
python -c "from app.shared.formulas import tobler_hiking_speed; print('OK')"

# Старые пути (должны показывать DeprecationWarning или работать)
python -c "from app.services.calculators import ToblerCalculator; print('OK')"
python -c "from app.models.user_profile import UserPerformanceProfile; print('OK')"
```

---

## Часть 4: План Phase 12 - Финальный Cleanup

### 4.1 Задачи

1. **Обновить CLAUDE.md** — новая структура, актуальные пути
2. **Проверить и почистить legacy файлы** — что re-export, что удалить
3. **Верифицировать все README.md** — актуальность
4. **Создать ARCHITECTURE.md** — если устарел
5. **Добавить integration тесты** — хотя бы smoke tests для endpoints

### 4.2 Legacy файлы: решение (ПРОВЕРЕНО 2026-01-28)

#### Критически используются (НЕ удалять):
| Файл | Причина |
|------|---------|
| `services/calculators/base.py` | Базовый класс для всех калькуляторов |
| `services/calculators/comparison.py` | Используется в predict.py для сравнения методов |
| `services/naismith.py` | old_naismith - 3-й метод расчёта |
| `services/prediction.py` | Кросс-фичёвый сервис оркестрации |
| `services/sun.py` | Расчёт восхода/захода, используется в prediction.py |
| `services/user_profile.py` | Расчёт профилей, используется в profiles.py |
| `models/prediction.py` | Модель БД для хранения предсказаний |
| `models/user_profile.py` | Главная модель профилей |
| `models/user_run_profile.py` | Модель для бегунов |

#### Чистый re-export (можно удалить после обновления импортов):
| Файл | Статус |
|------|--------|
| `services/gpx_parser.py` | Re-export из features/gpx/parser |
| `services/strava.py` | Re-export из features/strava |
| `services/strava_sync.py` | Re-export из features/strava/sync |
| `repositories/gpx.py` | Re-export из features/gpx/repository |
| `models/gpx.py` | Re-export из features/gpx/models |
| `models/notification.py` | Re-export из features/users/models |
| `models/strava_token.py` | Re-export из features/strava/models |
| `models/user.py` | Re-export из features/users/models |
| `models/strava_activity.py` | Re-export из features/strava/models |

#### Не существует:
| Файл | Статус |
|------|--------|
| `services/calculators/segmenter.py` | Файл не найден (перенесён в features/gpx/segmenter.py) |

---

## Часть 5: Рекомендации по CLAUDE.md

### Текущие проблемы:
1. Старая структура проекта
2. Устаревшие пути к файлам
3. "Известные проблемы" уже исправлены

### Что добавить:
1. Новая структура `features/`
2. Правильные пути к калькуляторам
3. Информация о shared/
4. Обновлённый список проблем
5. Ссылка на docs/codereview/ для истории

---

## Часть 6: Рекомендации по скиллу code-review

### Что добавить на основе опыта v2:

1. **В common-mistakes.md:**
   - Пример Phase 0c/0f: circular import при re-export из features в старые __init__.py
   - Решение: оставлять старые файлы как есть, делать re-export позже

2. **В migration-patterns.md:**
   - Добавить паттерн "кросс-фичёвый сервис" (prediction.py остался в services/)
   - Когда НЕ переносить в features/

3. **В SKILL.md:**
   - Добавить этап "Post-refactor cleanup" как обязательный
   - Чеклист: CLAUDE.md, README.md, legacy files

4. **Новый файл: post-refactor-checklist.md:**
   - Верификация импортов
   - Проверка тестов
   - Обновление документации
   - Ручное тестирование

---

## Часть 7: Порядок выполнения Phase 12

```
Phase 12a: Верификация (ручное тестирование)
     ↓
Phase 12b: Legacy files cleanup
     ↓
Phase 12c: Обновить CLAUDE.md
     ↓
Phase 12d: Обновить skill code-review
     ↓
Phase 12e: Финальная проверка
```

---

## Чеклист для следующей сессии

- [ ] Выполнить ручную проверку (3.2, 3.3)
- [x] Проверить какие legacy файлы реально используются (grep) ✅ 2026-01-28
- [x] Обновить CLAUDE.md ✅ 2026-01-28
- [x] Обновить docs/ARCHITECTURE.md ✅ 2026-01-28
- [ ] Обновить skill
- [ ] Создать коммит "refactor: phase 12 - post-refactor cleanup"

---

## Выполнено 2026-01-28

### Phase 12b: Анализ legacy файлов
- Проведён полный grep-анализ всех legacy файлов
- Выявлено 9 критически используемых файлов (НЕ удалять)
- Выявлено 9 чистых re-export файлов (можно удалить после миграции импортов)
- Файл `services/calculators/segmenter.py` не существует

### Phase 12c: Обновление документации
- CLAUDE.md полностью переписан под новую структуру features/
- docs/ARCHITECTURE.md обновлён с новыми путями к файлам
- Добавлены примеры новых импортов

---

*Created: 2026-01-28*
*Updated: 2026-01-28*
