# Step 5.6: Баги и фиксы перед деплоем

## ✅ Сделано

- [x] **1** Глобальный поиск — выпадающий список шире строки
- [x] **2** Сезон stats — "3 с результатами" → число участников
- [x] **7** DNF/DNS сортировались первыми → "Лучший" = 0
- [x] **8** Нормализация клубов (SkyRunGroup → Sky Run Group)
- [x] **9** Дёрганье ширины при переключении дистанций (остатки из-за длинных клубов — ок)
- [x] **3** Текст "горные и шоссейные" → "беговые" на странице гонок
- [x] **5** Повторный клик на год скрывает результаты — не должен
- [x] **6** Пустые дистанции (напр. Burabay Ice) — починилось
- [x] **7.2** DNF/DNS блок — вёрстка аватарка+имя в строку (добавлен dt-col-name-inner)
- [x] **16** "8 км" — увеличен до 14px
- [x] **18** Номер участника (bib) в таблице результатов
- [x] **21** Персентили участников в клубах — мельче и приглушённые
- [x] **22** Эстафета — перенос строки для длинных имён (white-space: normal на dt-col-name)

---

## 🔶 После мелких фиксов

- [x] **17+24** Убрать Алматы Марафон из вывода гонок (фильтр по `_am_kz` суффиксу в races + stats)
- [x] **4** Добавить ссылки на организаторов: Athletex (АМ закомментирован пока данные грязные)
- [x] **15** Убрать не-беговые дистанции (ski/skitour/splitboard, bike/mtb/velo/gravel). Nordic и snowshoe оставлены
- [x] **13+19** Фикс определения актуального клуба бегуна: `batch_parse.py` сортирует editions по году ASC (старые → новые), так что runner.club = клуб из последней гонки. Если последняя гонка без клуба — не обнуляем (считаем "забыл указать"). **Требует перепарсинг** `--all --force` (без AM). Глобальная сортировка между гонками → #29
- [ ] **20** Персентили на странице гонки (как у бегунов) — подумать над дизайном
- [x] **25** Backyard Ultra — хардкод реверс сортировки в API (`_BACKYARD_RACE_IDS`). Чистое решение → #27

---

## 📋 Отложено (после деплоя)

- [ ] **10** Поп-ап "Дисклеймер" на главной
- [ ] **11** Кнопка "Сообщить о неточностях" (телеграм / Google Form)
- [ ] **12** Страницы "Клубы" и "Бегуны" в навбар
- [ ] **14** Редизайн табов годов
- [ ] **23** Блоки гонок выглядят пустыми — ревизия дизайна
- [ ] **26** Система типов дистанций: добавить `discipline` в `race_distances` (run_trail, run_road, bike_mtb, ski_mountaineering, nordic_walking, snowshoe, canicross, relay, other). Миграция по ключевым словам. Перенести `RACE_TYPE_BY_ID` из фронт-хардкода в БД. Фильтровать не-беговые дисциплины нормально
- [ ] **28** Персентильная гистограмма — тавтология. Деление N финишёров на персентильные полосы (0-10%, 10-25%, ...) всегда даёт одинаковое пропорциональное распределение (~10/15/25/25/25%) для любой гонки, т.к. персентиль = место, а место делится равномерно по определению. **Варианты решения:** (a) TimeHistogram с персентильной раскраской баров, (b) компактная шкала-легенда "время → уровень" без bar chart, (c) убрать совсем — персентили уже есть в таблице участников. **Текущее состояние:** откачено на TimeHistogram, код закомментирован в `stats.py`, компонент `PercentileHistogram.tsx` сохранён для будущего. Схема `PercentileBucketSchema` и тип `PercentileBucket` оставлены
- [ ] **29** Глобальная сортировка при парсинге между гонками. Сейчас `batch_parse.py` сортирует editions по году внутри одной гонки, но **между гонками** порядок из `catalog.yaml` (рандомный). Если бегун участвовал в Alpine Race 2026 (клуб A) и Amangeldy Race 2026 (клуб B), то клуб зависит от порядка в yaml. **Решение:** при `--all` собрать все (race, edition) пары глобально и отсортировать по `edition.date` (дата есть в CLAX XML, сохраняется в `race_editions.date`). **Приоритет:** низкий — edge case когда бегун меняет клуб между гонками одного года
- [ ] **30** Клубы: показывать «за всё время» и «активные за текущий+прошлый год». `runners_count` в `clubs` считает all-time. Для active нужен запрос через `race_results → race_editions.year`. Пример: SkyRunGroup all-time=149, active 2025+=79. Показывать оба числа на странице клубов и в sidebar
- [ ] **27** Backyard Ultra — чистое решение. **Текущий хардкод:** `_BACKYARD_RACE_IDS` в 3 файлах (races.py, runners.py, stats.py — TODO). **Что нужно:** добавить `format` в `race_distances` или `races` (standard, backyard, relay). Пересчитать place в БД (или в парсере при импорте). Убрать все хардкоды. **Затронутые файлы:** `races.py` (get_results: реверс сортировки, search: инверсия percentile/gender_percentile/category_rank), `runners.py` (get_runner_profile: инверсия percentile, пересчёт place), `stats.py` (calculate_stats: best/worst_time, _club_stats: best=max — пока НЕ фиксили)
